using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class myIMGUI : MonoBehaviour
{
    // private  Rect = new windowRect(Screen.width / 2 , Screen.height / 2 , _buttonRectWidth, _buttonRectHeight);

    //void OnGUI()
    //{
    //windowRect = GUI.

    //void WindowFunc(ind windowID)
    //{
    //    if (GUI.Button(new Rect(Screen.width / 2 - _buttonRectWidth * 0.45f, Screen.height / 2 - _buttonRectHeight * 0.1f - _buttonRectHeight * 0.22f, _buttonRectWidth * 0.9f, _buttonRectHeight * 0.2f), "Continue"))
    //    {
    //        print( "Continue");
    //    }

    //    if (GUI.Button(new Rect(Screen.width / 2 - _buttonRectWidth * 0.45f, Screen.height / 2 - _buttonRectHeight * 0.1f - _buttonRectHeight * 0.22f, _buttonRectWidth * 0.9f, _buttonRectHeight * 0.2f), "Restart"))
    //    {
    //        print("Restart");
    //    }

    //    if (GUI.Button(new Rect(Screen.width / 2 - _buttonRectWidth * 0.45f, Screen.height / 2 - _buttonRectHeight * 0.1f - _buttonRectHeight * 0.22f, _buttonRectWidth * 0.9f, _buttonRectHeight * 0.2f), "Setting"))
    //    {
    //        print("Setting");
    //    }

    //    if (GUI.Button(new Rect(Screen.width / 2 - _buttonRectWidth * 0.45f, Screen.height / 2 - _buttonRectHeight * 0.1f - _buttonRectHeight * 0.22f, _buttonRectWidth * 0.9f, _buttonRectHeight * 0.2f), "Exit"))
    //    {
    //        print("Exit");
    //    }
    //}
    // }
}
